function initScene(){sceneApi?.dispose?.();const host=$('#scene');if(!host)return;const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(42,host.clientWidth/host.clientHeight,.1,100);camera.position.set(4.5,3.4,5.8);const renderer=new THREE.WebGLRenderer({antialias:true,alpha:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.setSize(host.clientWidth,host.clientHeight);renderer.outputEncoding=THREE.sRGBEncoding;renderer.shadowMap.enabled=true;host.prepend(renderer.domElement);const controls=new THREE.OrbitControls(camera,renderer.domElement);controls.enableDamping=true;controls.autoRotate=true;controls.autoRotateSpeed=.7;controls.target.set(0,.45,0);scene.add(new THREE.HemisphereLight(0xaedaff,0x10131a,2.2));const key=new THREE.DirectionalLight(0xffffff,3);key.position.set(4,6,3);key.castShadow=true;scene.add(key);const rim=new THREE.PointLight(0x36d9ff,24,12);rim.position.set(-3,2,-2);scene.add(rim);const floor=new THREE.Mesh(new THREE.CylinderGeometry(2.4,2.7,.18,64),new THREE.MeshStandardMaterial({color:0x14263a,metalness:.72,roughness:.25}));floor.position.y=-.15;floor.receiveShadow=true;scene.add(floor);const g=makeModel(state.designType,current());scene.add(g);let raf;const loop=()=>{raf=requestAnimationFrame(loop);controls.update();g.rotation.y+=.0015;renderer.render(scene,camera)};loop();const resize=()=>{if(!host.isConnected)return;camera.aspect=host.clientWidth/host.clientHeight;camera.updateProjectionMatrix();renderer.setSize(host.clientWidth,host.clientHeight)};window.addEventListener('resize',resize,{once:true});sceneApi={dispose(){cancelAnimationFrame(raf);renderer.dispose();controls.dispose()}}}
function mat(c,metal=.6,rough=.28){return new THREE.MeshStandardMaterial({color:c,metalness:metal,roughness:rough})}
function box(w,h,d,c,x=0,y=0,z=0){const m=new THREE.Mesh(new THREE.BoxGeometry(w,h,d),mat(c));m.position.set(x,y,z);m.castShadow=true;m.receiveShadow=true;return m}
function makeModel(type,d){const g=new THREE.Group();if(type==='cpu'){g.add(box(2.3,.22,2.3,0xbfc8cf,0,.4,0),box(2.05,.12,2.05,0x26303b,0,.25,0));for(let x=-.85;x<=.85;x+=.28)for(let z=-.85;z<=.85;z+=.28)g.add(box(.08,.08,.08,0xd7ad52,x,.14,z));const die=box(1.25,.08,1.25,0x2a94b8,0,.55,0);g.add(die)}else if(type==='gpu'){g.add(box(3.6,.18,1.7,0x173c2f,0,.25,0),box(3.35,.7,1.55,0x202b38,0,.68,0));for(const x of[-1.05,0,1.05]){const fan=new THREE.Mesh(new THREE.CylinderGeometry(.48,.48,.13,32),mat(0x0b1016,.2,.65));fan.rotation.x=Math.PI/2;fan.position.set(x,.7,.83);g.add(fan);for(let i=0;i<7;i++){const blade=box(.08,.04,.37,0x374756,x,.7,.92);blade.rotation.z=i*Math.PI/3.5;g.add(blade)}}g.add(box(.35,.35,.35,0x42d7e8,-1.4,.25,0))}else{const shell=box(2.5,3.3,2.25,0x172536,0,1.55,0);shell.material.transparent=true;shell.material.opacity=.92;g.add(shell);const glass=box(2.34,3.08,.05,0x4b99bd,0,1.55,1.14);glass.material.transparent=true;glass.material.opacity=.22;g.add(glass);g.add(box(1.65,.1,1.2,0x244c39,0,1.55,.45),box(.9,.15,.9,0xbec7ce,0,2.3,.48),box(1.7,.45,.55,0x26394a,0,.9,.72));for(let i=0;i<3;i++){const f=new THREE.Mesh(new THREE.TorusGeometry(.34,.055,10,30),new THREE.MeshStandardMaterial({color:0x42d7e8,emissive:0x145060,emissiveIntensity:1}));f.rotation.y=Math.PI/2;f.position.set(-1.27,.7+i*.85,0);g.add(f)}}return g}
render();
